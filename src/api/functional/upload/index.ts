/**
 * @packageDocumentation
 * @module api.functional.upload
 * @nestia Generated by Nestia - https://github.com/samchon/nestia 
 */
//================================================================
import { Fetcher } from "@nestia/fetcher";
import type { IConnection } from "@nestia/fetcher";
import typia from "typia";

import { NestiaSimulator } from "./../../utils/NestiaSimulator";
import type { IUpload } from "./../../structures/upload";

/**
 * 리소스를 업로드하기 위한 presigned url을 얻기 위한 요청
 * 
 * @return url and presigned url
 * @throws 500 Internal Server Error
 * 
 * @controller UploadController.getImageUrls()
 * @path POST /upload
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function getImageUrls(
    connection: IConnection,
    body: IUpload.IBody,
): Promise<getImageUrls.Output> {
    return !!connection.simulate
        ? getImageUrls.simulate(
              connection,
              body,
          )
        : Fetcher.fetch(
              connection,
              getImageUrls.ENCRYPTED,
              getImageUrls.METHOD,
              getImageUrls.path(),
              body,
              getImageUrls.stringify,
          );
}
export namespace getImageUrls {
    export type Input = IUpload.IBody;
    export type Output = IUpload;

    export const METHOD = "POST" as const;
    export const PATH: string = "/upload";
    export const ENCRYPTED: Fetcher.IEncrypted = {
        request: false,
        response: false,
    };

    export const path = (): string => {
        return `/upload`;
    }
    export const random = (g?: Partial<typia.IRandomGenerator>): Output =>
        typia.random<Output>(g);
    export const simulate = async (
        connection: IConnection,
        body: getImageUrls.Input,
    ): Promise<Output> => {
        const assert = NestiaSimulator.assert({
            method: METHOD,
            host: connection.host,
            path: path()
        });
        assert.body(() => typia.assert(body));
        return random(
            typeof connection.simulate === 'object' &&
                connection.simulate !== null
                ? connection.simulate
                : undefined
        );
    }
    export const stringify = (input: Input) => typia.assertStringify(input);
}